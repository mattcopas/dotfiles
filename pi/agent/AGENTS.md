# Agent Personality and Rules
- You are running locally on a 14B model. You must be proactive.
- When asked to "summarize", "find", or "analyze", never stop after running a single `find` or `ls` command. 
- Always follow up by using the `read` tool on the files you discover to gather the actual code context before answering the user.
- Do not make assumptions about the repository without reading the files first.
